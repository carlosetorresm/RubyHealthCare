<%if @dservice != [] %>
<table>
    <tr>
        <th>Nombre</th>
        <th style="width: 400px;">Descripción</th>
        <th>Costo</th>
        <th colspan="3"></th>
    </tr>
    <% @dservice.each do |s| %>
    <tr>    
        <td><%= Service.find_by(id: s.service).name %></td>
        <td><%= Service.find_by(id: s.service).description %></td>
        <td>$ <%= s.price %><br></td>
        <%if doctor_signed_in? %>
            <td><%= button_to "Eliminar",dservice_url(s), method: :delete %></td>
        <%end%>
    </tr>
    <%end%>
</table>
<%end%>